<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Banner ad sits at the bottom, above the WebView layer.
         A close (✕) button overlays the top-right corner so users can dismiss
         the banner at any time — required by Huawei AppGallery review policy. -->
    <FrameLayout
        android:id="@+id/bannerContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:visibility="gone">

        <!-- The BannerAdView is added here programmatically in MainActivity -->

        <TextView
            android:id="@+id/bannerCloseBtn"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="top|end"
            android:background="#CC000000"
            android:gravity="center"
            android:text="✕"
            android:textColor="#FFFFFF"
            android:textSize="13sp"
            android:clickable="true"
            android:focusable="true"
            android:elevation="4dp" />

    </FrameLayout>

    <!-- Privacy Policy button is a NATIVE view — GDevelop's WebView canvas
         cannot intercept touch events on views that are siblings/parents of
         the WebView in the native view hierarchy. -->
    <TextView
        android:id="@+id/privacyPolicyBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|start"
        android:layout_marginBottom="12dp"
        android:layout_marginStart="12dp"
        android:background="#8C000000"
        android:paddingHorizontal="10dp"
        android:paddingVertical="6dp"
        android:text="Privacy Policy"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:fontFamily="sans-serif"
        android:clickable="true"
        android:focusable="true" />

</FrameLayout>
