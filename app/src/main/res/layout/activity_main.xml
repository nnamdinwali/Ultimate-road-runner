<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!--
        MREC (300x250) ad container.
        Appodeal.show(activity, Appodeal.MREC) is only effective when an
        AppodealView with adType="mrec" exists in the view hierarchy.
        Without this view every show() call is a silent no-op.
        Centred horizontally, sits 60dp above the bottom banner strip.
    -->
    <com.appodeal.ads.AppodealView
        android:id="@+id/mrecView"
        android:layout_width="300dp"
        android:layout_height="250dp"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="60dp"
        android:visibility="gone"
        app:adType="mrec" />

</FrameLayout>

